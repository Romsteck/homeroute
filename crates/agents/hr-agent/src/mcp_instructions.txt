Dataverse MCP — ALWAYS use these tools for all database operations on /root/workspace/.dataverse/app.db. NEVER modify the database file directly. Use list_tables and describe_table to inspect before modifying. Schema tools: create_table, add_column, remove_column, create_relation, drop_table. Data tools: query_data, insert_data, update_data, delete_data, count_rows. For schema migration to PROD, use the deploy MCP tools: prod_schema, schema_diff, migrate_schema. NEVER use prod_push to copy .dataverse/ — use migrate_schema instead.